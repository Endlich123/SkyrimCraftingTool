<UserControl x:Class="SkyrimCraftingTool.MaterialsPanel"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:SkyrimCraftingTool">

    <StackPanel>

        <Button Content="Add Material"
                Command="{Binding AddMaterialCommand}"
                Width="120" Margin="0 0 0 10"/>

        <ItemsControl ItemsSource="{Binding MaterialList}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">

                        <ComboBox ItemsSource="{x:Static local:MainViewModel.MaterialNames}"
                                SelectedItem="{Binding Material, UpdateSourceTrigger=PropertyChanged}"
                                Width="150" />





                        <TextBox Text="{Binding Amount, UpdateSourceTrigger=PropertyChanged}"
                                 Width="60" Margin="10 0"/>

                        <Button Content="X"
                                Command="{Binding DataContext.RemoveMaterialCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}"
                                Width="25" Margin="10 0"/>

                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

    </StackPanel>
</UserControl>
