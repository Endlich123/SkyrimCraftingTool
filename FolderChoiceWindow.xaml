<Window x:Class="SkyrimCraftingTool.FolderChoiceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ordner auswählen" Height="200" Width="600">

    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <ScrollViewer x:Name="PART_ContentHost"/>
                            <TextBlock x:Name="Placeholder"
                                   Text="{TemplateBinding Tag}"
                                   Foreground="Gray"
                                   Margin="5,0,0,0"
                                   IsHitTestVisible="False"
                                   VerticalAlignment="Center"
                                   Visibility="Collapsed"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="Placeholder" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Placeholder" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>


    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- GameDataPath -->
        <StackPanel Orientation="Horizontal" Margin="0 5">
            <TextBox x:Name="GameDataPathBox" Width="450" Tag="choose your /Data Folder"/>
            <Button Content="..." Width="40" Margin="5 0" Click="SelectGameDataPath_Click"/>
        </StackPanel>
        <!-- ModDirectoryPath -->
        <StackPanel Orientation="Horizontal" Margin="0 5" Grid.Row="1">
            <TextBox x:Name="ModDirectoryPathBox" Width="450" Tag="choose Mod-Folder"/>
            <Button Content="..." Width="40" Margin="5 0" Click="SelectModDirectoryPath_Click"/>
        </StackPanel>
        <!-- PluginsFilePath -->
        <StackPanel Orientation="Horizontal" Margin="0 5" Grid.Row="2">
            <TextBox x:Name="PluginsFilePathBox" Width="450" Tag="plugins.txt MO2-> profile or \Users\youruser\AppData\Local\Skyrim Special Edition"/>
            <Button Content="..." Width="40" Margin="5 0" Click="SelectPluginsFilePath_Click"/>
        </StackPanel>
        <Button Grid.Row="3" Content="Speichern" Width="120" Height="30" HorizontalAlignment="Right" Click="SaveAndClose_Click"/>
    </Grid>
</Window>
